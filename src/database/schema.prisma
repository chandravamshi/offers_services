generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model OfferTemplate {
  id   Int    @id @default(autoincrement())
  body String
  type String
  name String
}
